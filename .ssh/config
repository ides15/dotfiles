Include ~/.config/colima/ssh_config

Include ~/.ssh/amazon.config

# Use 1Password as SSH agent for GitHub
Host *
  IdentityAgent "~/Library/Group Containers/2BUA8C4S2C.com.1password/t/agent.sock"

# IF THIS BREAKS ^ REPLACE WITH
#
# Host * !git.amazon.com
#
# Should work fine, since the amazon config is included first and the first
# match should be used
